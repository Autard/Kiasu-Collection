/* Global Reset/Defaults */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  font-family: Arial, sans-serif;
  background-color: #f5e4d7; /* Similar beige tone */
  color: #7b2a25; /* Maroon/dark red for text */
  line-height: 1.6;
}

/* Navigation */
header {
  background-color: #7b2a25; 
  padding: 1rem;
}
header ul {
  display: flex;
  justify-content: center;
  list-style: none;
}
header li {
  margin: 0 1.5rem;
}
header a {
  color: #f5e4d7;
  text-decoration: none;
  font-weight: bold;
}
header a:hover {
  text-decoration: underline;
}

/* HOW TO ORDER SECTION */
.how-to-order-section {
  padding: 2rem;
  text-align: center;
}
.how-to-order-container {
  max-width: 700px;
  margin: 0 auto;
}
.how-to-order-list {
  text-align: left;
  margin: 1rem 0;
  list-style: disc inside;
}
.order-button {
  display: inline-block;
  background-color: #7b2a25;
  color: #fff;
  padding: 0.75rem 1.5rem;
  text-decoration: none;
  border-radius: 5px;
  margin-top: 1rem;
}
.order-button:hover {
  background-color: #5f1f1b;
}

/* KIASU SECTION */
.kiasu-section {
  text-align: center;
  padding: 3rem 1rem;
}
.kiasu-title {
  font-size: 3rem;
  margin-bottom: 0.2rem;
  text-transform: lowercase;
}
.kiasu-subtitle {
  font-size: 1.2rem;
  letter-spacing: 2px;
}

/* FEATURED SECTION */
.featured-section {
  text-align: center;
  padding: 2rem 1rem;
}
.featured-gallery {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 1rem;
  margin-top: 1rem;
}
.featured-item img {
  width: 200px;
  height: auto;
  border: 2px solid #7b2a25;
  border-radius: 8px;
}

/* ORDER FORM SECTION */
.order-form-section {
  text-align: center;
  padding: 2rem 1rem;
}
.google-form-link {
  display: inline-block;
  background-color: #7b2a25;
  color: #fff;
  padding: 0.75rem 1.5rem;
  text-decoration: none;
  border-radius: 5px;
  margin-top: 1rem;
}
.google-form-link:hover {
  background-color: #5f1f1b;
}

/* CONTACT / CHAT SECTION */
.contact-section {
  text-align: center;
  padding: 2rem 1rem;
}

.chat-open-btn {
  background-color: #7b2a25;
  color: #fff;
  padding: 0.75rem 1.5rem;
  border: none;
  border-radius: 5px;
  cursor: pointer;
}
.chat-open-btn:hover {
  background-color: #5f1f1b;
}

/* Chat Box Styles */
.chat-box {
  position: fixed;
  bottom: 20px;
  right: 20px;
  width: 300px;
  max-width: 80%;
  background-color: #fff;
  border: 2px solid #7b2a25;
  border-radius: 8px;
  display: none; /* Hidden by default */
  flex-direction: column;
  box-shadow: 0 0 10px rgba(0,0,0,0.2);
}
.chat-header {
  background-color: #7b2a25;
  color: #fff;
  padding: 0.5rem;
  display: flex;
  justify-content: space-between;
  align-items: center;
  border-radius: 6px 6px 0 0;
}
.chat-close-btn {
  background: none;
  border: none;
  color: #fff;
  font-size: 1.2rem;
  cursor: pointer;
}
.chat-body {
  padding: 0.5rem;
  height: 200px;
  overflow-y: auto;
}
.chat-footer {
  display: flex;
  border-top: 1px solid #ccc;
}
.chat-footer input[type="text"] {
  flex: 1;
  padding: 0.5rem;
  border: none;
  outline: none;
}
.chat-footer button {
  background-color: #7b2a25;
  color: #fff;
  border: none;
  width: 60px;
  cursor: pointer;
}
.chat-footer button:hover {
  background-color: #5f1f1b;
}

/* FOOTER */
footer {
  background-color: #7b2a25;
  color: #fff;
  text-align: center;
  padding: 1rem;
  margin-top: 2rem;
}

